{
  "name": "tm-json-ics",
  "description": "Takes simple JSON for events and converts it to an ICS file.",
  "version": "1.3.0",
  "private": true,
  "dependencies": {
    "moment": "^2.10.2",
    "underscore": "^1.8.3"
  },
  "engines": {
    "node": ">= 0.8.x",
    "npm": ">= 1.1.x"
  }
}
